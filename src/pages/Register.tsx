import { useForm } from "react-hook-form"
import { DevTool } from "@hookform/devtools"

type FormValues = {
    username: string,
    email: string,
    password: string,
}

const Register = () => {
  return (
    <div>Register</div>
  )
}

export default Register